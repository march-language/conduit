mod Conduit.CronConfig do
  import Conduit
  fn default() : Conduit.CronConfig do
    {
      schedule = "* * * * *",
      timezone = "UTC",
      queue    = "default",
      overlap  = Conduit.CronOverlap.Skip,
      tags     = Nil
    }
  end
end
