[package]
name = "conduit"
version = "0.1.4"
type = "lib"
description = "A typed, crash-safe job queue and durable workflow engine for March — retries, cron, workflows, multi-node clustering, and a web dashboard"
author = "Chase Gilliam"
license = "MIT"

[deps]
depot = { registry = "forge", version = "0.3.2" }
