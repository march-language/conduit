-- DeterministicAppTest tests have been merged into ConduitTest
-- (test_conduit.march) to avoid a second impl Conduit.Storage block
-- which prevents the monomorphization pass from resolving polymorphic
-- storage call sites.
mod DeterministicAppTest do
end
